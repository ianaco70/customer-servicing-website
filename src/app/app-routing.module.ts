// vendor
import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

// containers

// components

// guards

const ROUTES: Routes = [];

export const routedComponents: Object[] = [];

@NgModule({
  declarations: [],
  exports: [RouterModule],
  imports: [RouterModule.forRoot(ROUTES)]
})
export class AppRoutingModule {}
